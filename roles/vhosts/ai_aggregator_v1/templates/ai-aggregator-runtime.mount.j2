[Unit]
Description=AI Aggregator volatile runtime filesystem
Before=new-api.service litellm.service

[Mount]
What=tmpfs
Where={{ ai_aggregator_runtime_root }}
Type=tmpfs
Options=mode=0700,size=128M,nosuid,nodev,noexec

[Install]
WantedBy=multi-user.target
