---
- name: Assert this role runs on a Darwin (macOS) host
  ansible.builtin.assert:
    that:
      - ansible_facts.os_family == "Darwin"
    fail_msg: "roles/vhosts/macos_migration only supports macOS (Darwin) hosts."
