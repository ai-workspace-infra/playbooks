{# Managed by Ansible: roles/vhosts/web_saas_host_config
   由 compose 栈各服务以绝对路径 env_file 读取。0600, 只有 root 可读。
   值全部来自流水线经 Vault OIDC 注入的环境变量, 本仓库不保存任何字面值。 #}
# Managed by Ansible: roles/vhosts/web_saas_host_config
# Do not edit by hand — regenerated on every deploy.
{% for key, value in web_saas_host_config_secrets | dictsort %}
{{ key }}={{ value }}
{% endfor %}
