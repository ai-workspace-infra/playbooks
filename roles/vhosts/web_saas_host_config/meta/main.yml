---
# 这个角色要 docker network create, 也要为一个 compose 栈准备落盘配置 ——
# 两件事都以 Docker Engine 存在为前提。
#
# 必须写全 vhosts/ 前缀: 只写 `docker` 也能解析, 但会命中 roles/docker,
# 那是个命名空间目录不是角色, 展开后只有 2 个任务而不是 20 个, 且不报错。
dependencies:
  - role: vhosts/docker
