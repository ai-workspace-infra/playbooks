---
- name: Install minimal XFCE desktop runtime
  ansible.builtin.import_tasks: install.yml
  tags: [xfce, xfce_install]

- name: Configure apt-managed Chrome browser
  ansible.builtin.import_tasks: browser.yml
  tags: [xfce, xfce_browser]

- name: Install AI workspace browser automation runtime
  ansible.builtin.import_tasks: runtime.yml
  tags: [xfce, xfce_runtime]
