---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart gitea
  ansible.builtin.systemd:
    name: gitea
    state: restarted

- name: Restart caddy
  ansible.builtin.systemd:
    name: caddy
    state: restarted
