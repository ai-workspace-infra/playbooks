{
	debug
}

# Agent Proxy virtual hosts are managed exclusively in conf.d. The wildcard
# import intentionally includes the historical tky-proxy.caddy compatibility
# fragment; that filename must remain loadable across production upgrades.
import /etc/caddy/conf.d/*.caddy
