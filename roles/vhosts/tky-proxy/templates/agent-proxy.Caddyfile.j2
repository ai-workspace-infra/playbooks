{
	debug
}

# Agent Proxy virtual hosts are managed exclusively in conf.d. Keeping the
# root file neutral prevents repeated deploys from declaring the same domain
# both here and in agent-proxy.caddy.
import /etc/caddy/conf.d/*.caddy
