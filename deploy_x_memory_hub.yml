---
# X Memory Hub — 轻量级 PostgreSQL-first、MCP 兼容的记忆中枢（开发版本，跟踪 main）。
# 依赖：postgresql.svc.plus 容器（pgvector/pg_jieba）+ LiteLLM 网关（embedding 复用）。
- name: Deploy X Memory Hub
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: roles/vhosts/x_memory_hub/
